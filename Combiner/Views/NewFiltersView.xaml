<UserControl x:Class="Combiner.NewFiltersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Combiner"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<StackPanel>
		<StackPanel Orientation="Horizontal">
			<ComboBox ItemsSource="{Binding StatFilterChoices}" 
					  Width="150"
					  IsEditable="False"
					  SelectedItem="{Binding SelectedStatFilter}"/>
			<Button Content="Add" Width="30" Command="{Binding AddStatFilterCommand}"/>
		</StackPanel>
		<ItemsControl ItemsSource="{Binding ChosenStatFilters}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<StackPanel/>
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate DataType="{x:Type local:StatFilter}">
					<local:StatFilterView/>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</StackPanel>
</UserControl>
