<UserControl x:Class="Combiner.Views.FilterViews.StatFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Combiner"
             xmlns:views="clr-namespace:Combiner.Views"
             mc:Ignorable="d">
	<StackPanel Orientation="Horizontal">
		<Label Content="{Binding Name}"/>
		<TextBox Width="50" Text="{Binding MinValue, Mode=TwoWay}"/>
		<Label>-</Label>
		<TextBox Width="50" Text="{Binding MaxValue, Mode=TwoWay}"/>
		<Button Content="Remove"
				Width="50"
				Command="{Binding DataContext.DropFilterCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type views:FiltersView}}}"
				CommandParameter="{Binding .}"/>
	</StackPanel>
</UserControl>
